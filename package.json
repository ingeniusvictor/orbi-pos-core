{
  "name": "orbi-pos-core",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --workspace=@orbi/pos-web run dev",
    "build": "npm --workspace=@orbi/pos-web run build",
    "test": "npm --workspace=@orbi/pos-web run test"
  }
}
